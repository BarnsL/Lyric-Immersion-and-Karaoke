# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# Build / logs
*.log
preload.log

# Spotify auth — local secrets / tokens, never commit
spotify_config.json
.spotify_cache

# Cached song lyrics are copyrighted third-party content — not redistributed.
# Run `python preload.py` (or just play music) to build your own local library.
lyrics/*.json
!lyrics/.gitkeep

# OS
Thumbs.db
desktop.ini
.DS_Store

# Local UI preferences
settings.json

# Local agent handoff notes (not for the public repo)
AGENT_HANDOFF.md
_icon_preview.png
_preview_cur.png
_exe_icon.png

# Build artifacts
build/
dist/
*.exe
*.spec.bak
lyrics/*.tmp
lyrics/*.json.tmp
